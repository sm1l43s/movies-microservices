spring:
  application:
    name: movies-service
    id:

server:
  port: 0

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.value}
  client:
    service-url:
      default-zone: http://localhost:8716/eureka