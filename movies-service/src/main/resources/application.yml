spring:
  application:
    name: movies-service
    id:

server:
  port: ${APP_PORT:0}

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.value}
  client:
    service-url:
      default-zone: https://discovery-service-pifj.onrender.com/eureka