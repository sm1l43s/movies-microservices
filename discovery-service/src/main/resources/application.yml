server:
  port: 8761

spring:
  application:
    name: discovery-service

eureka:
  instance:
    hostname: https://eureka-discovery-service.onrender.com
  client:
    register-with-eureka: false
    fetch-registry: false